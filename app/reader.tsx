import { Text, View } from 'react-native';

export default function ReaderPlaceholder() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="font-medium text-text">Reader (placeholder)</Text>
    </View>
  );
}
